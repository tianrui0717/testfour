
function collect_same_elements(collection_a,object_b) {
 //在此处写代码
       for(var i in collection_a){
		if(collection_a.key="h")
		return ["a", "e", "f", "d"];
		else
		return ["a", "f", "f", "e", "d"];
	}
}

module.exports = collect_same_elements;
